/**
 * Internal dependencies
 */
import { name } from '../../../../../assets/src/block-editor/blocks/buttons';

describe( 'blocks: material/buttons', () => {
	describe( 'name', () => {
		it( 'should equal material/buttons', () => {
			expect( name ).toStrictEqual( 'material/buttons' );
		} );
	} );
} );
